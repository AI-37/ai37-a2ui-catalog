/**
 * Ширины **корня набора** (`.a2ui-kit`), на которых форма меняет число колонок.
 * Меряем по корню, как нынешний `ConstructionsEditor` по своему `.a2ui-ce`:
 * сама форма уже внутри отступов карточки и её тела, это на ~60px меньше, и
 * порог, замеренный по ней, отсекал бы вторую колонку раньше, чем нужно.
 *
 * `TWO` = две колонки по ~220px плюс отступы карточки: сообщение шириной 500px
 * в чате — уже широкое, и одна колонка в нём читается как мобильная вёрстка на
 * десктопе.
 */
export const FORM_TWO_COLUMNS_AT = 500;

/**
 * `THREE` = «строка + два числа» в одну строку. Порог выше двух колонок: под
 * длинное название материала из справочника нужно место сверх двух коротких
 * числовых полей.
 */
export const FORM_THREE_COLUMNS_AT = 620;
