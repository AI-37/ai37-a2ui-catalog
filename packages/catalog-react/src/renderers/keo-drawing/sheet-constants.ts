/**
 * Чертёжные константы листа — то, чего в модели нет и быть не должно:
 * толщины плит и вспомогательных стен на картинке, поля под размерные линии,
 * целевая ширина листа. Метры, кроме явно названного в пикселях.
 */

/** Плиты пола и потолка на разрезе. */
export const SLAB_THICKNESS = 0.22;

/** Дальняя (глухая) стена на разрезе. */
export const FAR_WALL_THICKNESS = 0.4;

/** Боковые и дальняя стены на плане. */
export const PLAN_SIDE_WALL = 0.3;

/** Поле слева на разрезе под три вертикальные размерные линии. */
export const SECTION_LEFT_MARGIN = 2.4;

/** Целевая ширина листа, px: от неё считается цена деления. */
export const TARGET_SHEET_WIDTH = 720;

/** Цена деления не выходит за эти пределы, px на метр. */
export const MIN_PX_PER_METER = 34;
export const MAX_PX_PER_METER = 90;
