import type {ConstructionType} from '@ai37/a2ui-catalog-schemas';

/**
 * Единственный тип с разновидностями (чердачные/цокольные/пол по грунту):
 * селект subtype показывается только у него.
 */
export const SUBTYPED_TYPE: ConstructionType = 'cherdachnye_podval_grunt';
