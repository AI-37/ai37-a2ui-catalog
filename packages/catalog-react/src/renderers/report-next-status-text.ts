/**
 * Слова состояния, сводимого к перечислению. Зашиты в рендерер, а не приходят
 * строкой: состояние названо перечислением, и разрешить агенту выбирать для
 * него слово — значит разрешить двум отчётам говорить об одном и том же
 * по-разному (`statusLabel` чтится только вне перечисления).
 */
export const REPORT_NEXT_STATUS_TEXT = {
  pass: 'Соответствует',
  fail: 'Не соответствует',
} as const;

export type ReportNextStatus = keyof typeof REPORT_NEXT_STATUS_TEXT;
