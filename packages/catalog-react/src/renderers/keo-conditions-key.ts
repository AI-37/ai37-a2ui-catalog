/**
 * Ключ секции «Условия». Помещения и их секции ключуются локальным id
 * помещения (`calcTouchedKey`), а условия к помещению не относятся — им нужен
 * свой, не пересекающийся с id.
 */
export const KEO_CONDITIONS_KEY = 'conditions';
