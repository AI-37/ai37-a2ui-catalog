/**
 * Нижняя граница списка редакторов `*Next`: последний элемент не удаляется —
 * документ без помещений или без лифтов не расчёт, а пустой бланк.
 *
 * Правило добавления (`resolveAddItemState`) сверяет с этой границей верхний
 * предел: когда они сошлись, кнопка добавления мертва навсегда.
 */
export const MIN_EDITOR_ITEMS = 1;
