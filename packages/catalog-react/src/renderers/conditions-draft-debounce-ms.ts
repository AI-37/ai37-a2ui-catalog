/**
 * Окно дебаунса автодрафта условий: серия правок схлопывается в одну отправку
 * после паузы ввода (Решение 1 design constructions-editor-live-draft).
 * Порядок сотен мс; значение подбирается на демо.
 */
export const CONDITIONS_DRAFT_DEBOUNCE_MS = 500;
