## 0. Решения до кода

- [x] 0.1 Раскрывашка протокола — `<details>` или `Collapsible` набора (`design.md`, Решение 5) → `Collapsible` (Решение 9)
- [x] 0.2 Нужен ли рядом нынешний рендерер отчёта для сравнения «было / стало» (вопрос 2) → нет (Решение 10)
- [x] 0.3 Таблица слоёв — новый примитив или `renderers/simple-table` (вопрос 3) → свой примитив (Решение 11)

## 1. Наполнение

- [x] 1.1 Загрузить `fixtures/valid/thermal-report-single.json`, `thermal-report-multi.json`, `lift-report.json`; выдуманных данных не добавлять
- [x] 1.2 Разбирать схемами компонентов, а не приводить типом
- [x] 1.3 Убедиться, что три наполнения покрывают все секции: проверки, конструкции, исключённые, допущения, таблица слоёв, «Что изменить», группа `tone: 'warning'`, оба вида протокола

## 2. Примитивы, которых не хватает

- [x] 2.1 Строка списка: слот `main` (title + detail) и слот `side` (статус / чип / кнопка) — `ReportRow`
- [x] 2.2 Двухчастный чип «label value» и его пунктирный вариант — `DataChip`
- [x] 2.3 Статусная пилюля с точкой: тона `pass` / `fail` / `warning`, две ступени кегля — `StatusPill`
- [x] 2.4 Serif-заголовок: ось семейства у ступени `display`, а не новая ступень — `ReportHeadline` + `a2ui-t--serif`
- [x] 2.5 Таблица: колонки, числа справа, итоговая строка на утопленном фоне, скролл внутри контейнера — `ReportTable`
- [x] 2.6 Заметка с точкой на утопленном фоне — `ReportNote`
- [x] 2.7 Добавить всё перечисленное на `/proba/system` с осями и состояниями — блок «Отчёт»
- [x] 2.8 Проверить, что кнопки и «Скачать ▾» берутся готовыми (`Button`, `Menu`) и ничего не дописывается

## 3. Страница `/proba/report-assembly`

- [x] 3.1 Роут, пункт переключателя разделов, страница по образцу `/proba/lift-assembly`
- [x] 3.2 Теплотех, одна конструкция: вердикт → проверки → таблица слоёв → исходные → протокол
- [x] 3.3 Теплотех, список: вердикт → конструкции с отклонениями → исключённые → допущения → исходные → протокол
- [x] 3.4 Лифты: вердикт → «Что изменить» → исходные → протокол
- [x] 3.5 Отклонение чипом по знаку `deviationPct`; знак и тон считаются из значения, а не приходят строкой
- [x] 3.6 Группа `tone: 'warning'`: пунктирные чипы, предупреждающий заголовок, `note` под чипами
- [x] 3.7 Рекомендуемый вариант «Что изменить» (`tone: 'pass'`) выделен, непроходящий несёт `statusLabel`
- [x] 3.8 Кнопки-действия диспатчат в консоль `{name, payload}`; служебного трафика на экране нет
- [x] 3.9 Протокол одной строкой без раскрытия (Решение 15), «Скачать ⌄» — меню форматов, растущее вверх (Решение 16)
- [x] 3.10 Ветвления разметки — отдельными компонентами с ранним `return null`

## 4. Приёмка

- [x] 4.1 Проход клавиатурой: `Tab` по порядку чтения, кнопки-действия достижимы, «Скачать» открывается с клавиатуры, `Escape` закрывает меню
- [x] 4.2 Проверить `aria-expanded` у триггера «Скачать» и `aria-label` у кнопок без подписи (кнопок без подписи на экране нет)
- [x] 4.3 Консоль: каждое действие — одно сообщение с `{name, payload}`
- [x] 4.4 Широкая таблица слоёв прокручивается внутри карточки; страница по горизонтали не едет
- [x] 4.5 Скриншоты всех трёх наполнений в двух ширинах контейнера — `preview/`
- [x] 4.6 Положить рядом теплотех и лифты и выписать оставшиеся расхождения — Решения 12, 13, 14
- [x] 4.7 Проверить, что в `/proba/system` нет примитива, которого не видно на витрине, и наоборот
- [x] 4.8 Обновить `apps/demo/src/proba/README.md`: дерево сборки третьего экрана
- [x] 4.9 Убедиться, что `packages/catalog-react` не тронут
