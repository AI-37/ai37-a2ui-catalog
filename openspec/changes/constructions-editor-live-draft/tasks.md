# Tasks

## 1. Дебаунс-автодрафт условий

- [x] 1.1 `constructions-editor.tsx`: дебаунс-обёртка над `sendDraft` для
      правок условий (константа окна, trailing edge, payload из state на
      момент отправки); `handleGeneralChange` планирует отправку
- [x] 1.2 Submit сбрасывает таймер дебаунса (`handleSubmit`); unmount —
      таймер очищается
- [x] 1.3 Тесты: серия правок → один action; правки с паузой → два; submit
      до истечения окна → draft не уходит; выбор города из lookup →
      автозаполненный климат в одном action'е

## 2. Удаление кнопки «Сохранить» и dirty-логики

- [x] 2.1 `constructions-editor-conditions.tsx`: удалить `dirty`/`onSave` и
      кнопку; `constructions-editor.tsx`: удалить `conditionsDirty`
- [x] 2.2 Тест-регресс: кнопка сохранения не рендерится ни в каком состоянии

## 3. Живой ГСОП

- [x] 3.1 Убрать гейт `showGsop={!climateDirty}` — ГСОП рендерится всегда
      (значение из props либо прочерк)
- [x] 3.2 Тест: после replace-снапшота с новым `general.gsop` поле
      показывает новое значение, введённые поля не изменились

## 4. Верификация

- [x] 4.1 Демо-прогон: «Москва, tв 20» → ГСОП и Rнорм-бейджи обновились без
      кнопки; ввод не мигает и не затирается ответом
- [x] 4.2 E2E на стенде teplo: draft-конверт в истории chat-backend
      обновляется при вводе условий (без структурных правок)
- [x] 4.3 Minor bump + CHANGELOG; lint/typecheck/тесты пакета зелёные
