## 1. Канонический ключ документа

- [x] 1.1 `keoDocumentKey`: сортировка пар условий и значений помещений
- [x] 1.2 `undefined`-значения отбрасываются до сериализации
- [x] 1.3 Имена помещений и `note` — по-прежнему вне ключа

## 2. Ack по последнему черновику

- [x] 2.1 `lastDraftKey` пишется при каждой отправке черновика (рядом с дедупом `lastDraft`)
- [x] 2.2 Guard пересева: снапшот с ключом последнего черновика ИЛИ живого состояния — ack, пересев пропускается
- [x] 2.3 Настоящее новое сообщение агента пересевает состояние, как прежде

## 3. Тесты

- [x] 3.1 Эхо с другим порядком ключей значений — состояние экрана не сброшено
- [x] 3.2 Эхо, отставшее от живого ввода, — свежие нажатия не стёрты, секции раскрыты
- [x] 3.3 Прогон всего `tests/react/keo-editor-next.test.tsx` — прежние сценарии не сломаны
- [x] 3.4 Мутационная проверка: без правок guard'а новые тесты падают

## 4. Проверка глазами

- [x] 4.1 Репро в SP-AI (быстрый ввод в «Коэффициенты приняты по умолчанию» после перезагрузки страницы): секция не схлопывается, фокус живёт — проверено headless-репро 2026-08-29 на тарболлах 0.30.0 (install:consumer): панель и DOM-узел переживают эхо черновика, фокус в поле, ввод по четырём полям с несколькими round-trip'ами не теряется
