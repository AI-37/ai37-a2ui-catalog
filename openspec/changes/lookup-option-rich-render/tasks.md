## 1. Контракт опции (catalog-schemas)

- [x] 1.1 `form-card-lookup.types.ts`: необязательные `group` / `title` /
      `meta` в `LookupOption` с комментарием «слоты оформления; `label`
      остаётся обязательным и самодостаточным — он попадает в поле после
      выбора»
- [x] 1.2 Проверить, что тип не участвует в zod-схемах props и python-зеркале
      (`catalog-python` не трогается)

## 2. Состояние запроса (catalog-react)

- [x] 2.1 `use-lookup-suggest.ts`: вернуть `loading` и `queried` рядом с
      `options`; `loading` поднимается при прохождении порога `minChars`
      (включая паузу debounce), снимается ответом, ошибкой или падением ниже
      порога; отмена предыдущего запроса `loading` не гасит
- [x] 2.2 `closeOptions` сбрасывает и статусы — закрытый попап не должен
      «помнить» пустой результат

## 3. Рендер опции (catalog-react)

- [x] 3.1 Выделить рендер одной опции в отдельный модуль
      (`lookup-option-row.tsx`): многострочная раскладка group/title/meta,
      fallback на однострочный `label` при отсутствии `title`
- [x] 3.2 Подсветка: хелпер «первое вхождение подстроки без учёта регистра →
      три части» (`toLowerCase().indexOf()`, без RegExp), жирным — средняя
      часть; текст рендерится как текст, `dangerouslySetInnerHTML` не
      используется
- [x] 3.3 `lookup-combobox.tsx`: попап открыт при `loading || options.length >
      0 || queried`; статус-строка «Ищем…» / «Ничего не найдено» вне
      `<ul role="listbox">` с `role="status"`; `aria-busy` на контейнере при
      загрузке; `aria-expanded` инпута — по факту открытого попапа
- [x] 3.4 Стили многострочной опции в общем стилевом модуле (без инлайна там,
      где рядом уже класс) — group мельче и приглушённее, meta приглушённая,
      высота строки не ломает `maxHeight: 240` попапа

## 4. Сохранность доп. полей

- [x] 4.1 Тест `parse-lookup-options`: опция с `group`/`title`/`meta` и
      произвольным лишним полем доезжает целиком; элемент без строкового
      `value` отбрасывается

## 5. Тесты рендерера (tests/react)

- [x] 5.1 Многострочный рендер: три слота → три строки; без `group` — две;
      без `title` — одна строка `label`
- [x] 5.2 Выбор многострочной опции кладёт в поле `label`, а не `title`
- [x] 5.3 Подсветка: «кир» подсвечивает «Кир» в group с сохранением регистра;
      без совпадения — без подсветки; спецсимволы во вводе не ломают рендер;
      `<b>` в данных виден как текст
- [x] 5.4 Состояния: индикатор виден в течение debounce (таймеры замокать);
      ответ вытесняет индикатор; пустой ответ даёт «ничего не найдено»; ввод
      ниже порога закрывает попап
- [x] 5.5 a11y: во время загрузки внутри `role="listbox"` нет `role="option"`;
      статус имеет `role="status"`; `aria-busy` появляется и снимается
- [x] 5.6 Смешанная выдача (со слотами и без) рендерится одним списком

## 6. Демо и документация

- [x] 6.1 `apps/demo`: справочник с заполненными слотами (материалы прил. М —
      реалистичный пример) и справочник без слотов рядом
- [x] 6.2 README пакета/описание компонента: слоты как оформление, `label` —
      источник значения

## 7. Проверка

- [x] 7.1 `npm run typecheck && npm run lint && npm test` в репозитории
      каталога
- [x] 7.2 Ручная проверка в демо: набрать «кир» при подключённом справочнике
      материалов — виден индикатор, затем многострочные опции с подсвеченным
      «Кир»
