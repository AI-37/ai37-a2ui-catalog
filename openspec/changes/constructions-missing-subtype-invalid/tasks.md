## 1. Причина невалидности

- [x] 1.1 `find-invalid-layers.ts`: `subtypeRequired` из `config.alphaN`,
      `missingSubtype` в `ConstructionInvalidity`
- [x] 1.2 `invalid` учитывает новую причину
- [x] 1.3 `missingLambdaCount` обнуляется при `missingSubtype` — иначе чип
      соврёт «N слоёв без λ» при заполненных слоях

## 2. Текст в шапке раскрытой карточки

- [x] 2.1 `constructions-next-header-summary.tsx`: при отсутствии
      разновидности у типа, которому она обязательна, вместо пустого места —
      «разновидность не выбрана» с `a2ui-t--warning`
- [x] 2.2 Признак обязательности — тот же, что в 1.1 (конфиг типа), а не
      отдельная проверка
- [x] 2.3 Разновидность выбрана → печатается она, как прежде
- [x] 2.4 Тип, которому разновидность не положена → ни текста, ни пустого «·»

## 3. Тесты

- [x] 3.1 αн записью, разновидность не выбрана, слои валидны → `invalid: true`,
      `missingLambdaCount: null`
- [x] 3.2 αн записью, разновидность выбрана → `invalid: false`
- [x] 3.3 αн числом, разновидности нет → `invalid: false`
- [x] 3.4 Конфига типа нет вовсе → поведение прежнее
- [x] 3.5 Тест рендерера: свёрнутая карточка без разновидности — рамка помечена
      и чип «проверить»
- [x] 3.6 Тест рендерера: раскрытая карточка называет «разновидность не
      выбрана»; с выбранной — печатает её

## 4. Гейт

- [x] 4.1 `pnpm run typecheck`, `pnpm run test:ts`
- [x] 4.2 `pnpm run export:public && pnpm run verify:public`, `public/` закоммичен
- [x] 4.3 Скриншоты «до / после» в `preview/`: список из 8 конструкций
      свёрнуто И раскрытая карточка с текстом, обе ширины контейнера
- [x] 4.4 `pnpm run version:bump <x.y.z>` + раздел в `CHANGELOG.md` — тем же PR
- [x] 4.5 `pnpm run install:consumer ../spai-ui`, перезапуск dev-сервера
