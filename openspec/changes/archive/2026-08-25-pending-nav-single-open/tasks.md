# Tasks

## 1. Одиночное раскрытие при навигации

- [x] 1.1 `constructions-editor.tsx`: в `handlePendingClick` ветка карточки —
      `dismissStatusOnView(target.id)` + `setOpenIds(new Set([target.id]))`
      вместо `handleToggle`; ветка условий не меняется
- [x] 1.2 Тесты: «Далее» → раскрыта только целевая; повторное «Далее» →
      раскрыта только следующая, предыдущая «готова» и свёрнута; ручной
      аккордеон мульти-open; навигация к условиям карточки не трогает

## 2. Верификация

- [x] 2.1 Демо-прогон / стенд: «Далее» по трём пресетам — на экране каждый
      раз одна раскрытая карточка, статусы гаснут, в конце «Рассчитать»
- [x] 2.2 CHANGELOG (в 0.18.0, если он ещё не опубликован в Verdaccio, иначе
      minor bump 0.19.0); lint/typecheck/тесты пакета зелёные
