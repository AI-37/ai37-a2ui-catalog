# tasks — keo-report-chip-subscripts

## 0. Пререквизит

- [x] 0.1 Хелпер `renderLabelSubscripts` влит (`next-label-subscripts`)

## 1. Карточка «Исходные данные»

- [x] 1.1 `keo-report-inputs.tsx`: `chip.label` через хелпер
- [x] 1.2 `group.label` и `group.note` через хелпер; `chip.value` — как есть

## 2. Тесты

- [x] 2.1 Чип `d_п · b_п · h, м` рендерится с `<sub>`, `_` не виден
- [x] 2.2 Чип `Ориентация` и значения чипов — байт в байт как раньше
- [x] 2.3 Строка с `$...$` (агент ещё не обновился) проходит без изменений —
      хелпер доллары не трогает

## 3. По факту кода (design)

- [x] 3.1 `*Next`-путь: примитив `DataChip`, `report-next-input-group`,
      `report-next-group-note` — спека называла только старый рендерер
      (design, Решение 1)
- [x] 3.2 Заметка допущений и подпись кнопки варианта — тем же правилом, в
      обоих рендерерах (design, Решение 2)
- [x] 3.3 Подпись кнопки в обёртке: `.a2ui-btn` — inline-flex с зазором;
      тест по структуре, а не по тексту (design, Решение 3)
- [x] 3.4 На экране отчёта КЕО не осталось ни одного `_`; протокол не тронут
- [x] 3.5 Теплотех и лифт: общие примитивы, подписи без `_` — изменений нет
