## 1. Развилка до кода

- [x] 1.1 Проверить цели по браузерам у потребителей (`sp-ai-ui` и форки) и
      подтвердить `light-dark()`. Если цель ниже Chrome 123 / Safari 17.5 /
      Firefox 120 — перейти на отступной вариант Решения 1 (второй массив +
      тест на паритет ключей) и отметить это здесь
      → `light-dark()` принят. Явного `browserslist` нет ни здесь, ни в
      `spai-ui`; CSS набора не проходит сборку вовсе — он уезжает строкой в
      `<style>` из `KitStyles`, поэтому downlevel'ить его нечему и вопрос
      чисто рантаймовый. Потребитель — Next 16 + React 19 + Tailwind v4
      (сам требует Safari 16.4+ / Chrome 111+ / Firefox 128+) и уже полагается
      на `color-scheme` (`app/globals.css`). Ни одна объявленная цель
      `light-dark()` не запрещает; отступной вариант не понадобился
- [x] 1.2 Забрать тёмную половину палитры из Figma SP-AI, если она там есть
      (файл `1haPdZgyzP7Pm3k4Z6z5xU`); чего нет — вывести и пометить как
      выведенное
      → Взята из зеркала коллекции «SP-AI / Semantic» (моды Light/Dark) в
      `spai-ui/app/globals.css` (`:root` / `.dark`) — в самом файле сказано, что
      он выровнен по этому макету, и там же записаны замеры и расхождения. В
      Figma напрямую не ходил. Выведено ровно одно значение — `danger`
      в тёмной половине (см. `contrast.md`)

## 2. Слой токенов

- [x] 2.1 `kit-tokens.ts`: цветные токены — парами `light-dark(<светлое>,
      <тёмное>)`; нецветные (шрифты, шкала, радиусы) не трогать
- [x] 2.2 Токены-ссылки (`--a2ui-btn-*`, `--a2ui-chip-*`, `--a2ui-control-*`,
      `--a2ui-card-danger`) оставить ссылками — собственной пары им не заводить
- [x] 2.3 `--a2ui-shadow-popup` парой; `POPUP_TOKEN_NAMES` дополнить им
      → токен несёт только цвет тени: `light-dark()` — функция цвета, целый
      `box-shadow` в неё не кладётся, геометрия осталась в правиле
- [x] 2.4 Шапку `kit-tokens.ts` переписать под новый механизм: сегодня она
      обещает перекраску «объявив токен у себя на корне» — то, чего сделать
      нельзя

## 3. Выбор темы

- [x] 3.1 `kit-tokens-css.ts`: `:where([data-a2ui-theme='light'])` →
      `color-scheme: light`, `:where([data-a2ui-theme='dark'])` →
      `color-scheme: dark`
- [x] 3.2 Убедиться, что `.a2ui-kit` не объявляет `color-scheme` (Решение 2)
      → закреплено тестом
- [x] 3.3 `popup-css.ts`: тень — токеном; литералов цвета в файле не осталось
- [x] 3.4 Попап получает `color-scheme` от якоря при открытии
      (`getComputedStyle(anchor).colorScheme`) — в общем узле попапа, один раз
      на три примитива (`lookup`, `select`, `menu`)
      → общий узел — хук `use-popup-scheme.ts`: якорь и портальный узел живут в
      разных поддеревьях, общего DOM-предка у них нет

## 4. Значения и контраст

- [x] 4.1 Тёмные значения подставлены; источник каждого помечен (макет или
      выведено)
- [x] 4.2 Контраст посчитан по всем парам «текст / его подложка» в обеих темах:
      основной 4.5:1, крупный и границы 3:1. Таблица результатов — в PR
      → `contrast.md`. Тёмная половина порог проходит везде. В **светлой**
      половине три значения ниже порога (`danger` на утопленной, `success` на
      обеих, `warning` на утопленной) — они не тронуты этим изменением и были
      такими же до него; предложение по замене — в конце таблицы
- [x] 4.3 Акцент и семантические цвета (`danger`, `success`, `warning`) в
      тёмной половине заменены на прошедшие порог

## 5. Проверка вживую

- [x] 5.1 Демо: тумблер ставит `data-a2ui-theme` на тот же узел, что и
      `.a2ui-theme-dark`; обе семьи темнеют одновременно
      → `preview/dark-both-families.png`
- [x] 5.2 `/proba/system`: набор примитивов показан в обеих темах
      → тумблер в раме витрины ставит `data-a2ui-theme` на `<main>` — тем же
      способом, каким тему отдаёт потребитель; оформление рамы объявлено парами
      `light-dark()`. `preview/proba-system-light.png`,
      `preview/proba-system-dark.png`
- [x] 5.3 Скриншоты headless Chrome в `preview/`: тёмная тема на
      `ConstructionsEditorNext`, `LiftEditorNext`, `ThermalReportNext`,
      `LiftReportNext`; отдельно — открытый попап справочника и открытое меню
      «Скачать ⌄»
      → `dark-constructions-editor-next.png`, `dark-lift-editor-next.png`,
      `dark-thermal-report-next.png`, `dark-lift-report-next.png`,
      `dark-lookup-popup.png`, `dark-menu-download.png`
- [x] 5.4 Форма в тёмной теме: поля, фокус, подсветка активной строки,
      скроллбар внутри карточки отчёта (`color-scheme` красит и системные части
      — Risks)
      → поля и кольцо фокуса — на скриншотах попапа; подсветка активной строки
      резольвится в тёмную (`#242423` на подложке попапа `#2a2a2f`), но разница
      к подложке та же, что и в светлой (1,09 против 1,13) — приём слабый в
      обеих темах, отдельного решения здесь не принималось. Скроллящихся мест
      внутри карточек в фикстурах нет — единственное живое проверено на попапе
      (`dark-popup-scrollbar.png`); скроллбар macOS оверлейный и в покое не
      рисуется, красит его `color-scheme: dark`, который на узле стоит
- [x] 5.5 Узкая раскладка в тёмной теме — пороги контейнера не задеты
      → `preview/dark-narrow-constructions.png` (корень набора 312px:
      одна колонка, ничего не разъехалось)

## 6. Тесты и документация

- [x] 6.1 Тест: внутри `data-a2ui-theme="dark"` у корня набора computed
      `--a2ui-text-color` — тёмное значение; без атрибута — светлое
      → в jsdom так не проверить: он не реализует ни `light-dark()`, ни
      наследование `color-scheme` (`getComputedStyle` отдаёт пару как есть,
      `colorScheme` пустой). В тесте закреплено то, из чего складывается
      результат: пара в токене и правило выбора. Сама резольвинг-проверка
      сделана в браузере — под `data-a2ui-theme="dark"` у всех семи корней
      `.a2ui-kit` computed `color` = `rgb(237, 237, 234)`, фон карточки =
      `rgb(25, 25, 24)`, у портального попапа `color-scheme` = `dark`
- [x] 6.2 Тест: в `primitives` нет литералов цвета (исключая `kit-tokens.ts`)
- [x] 6.3 Тест: `POPUP_TOKEN_NAMES` — подмножество имён `KIT_TOKENS`
- [x] 6.4 Тесты старых рендереров не трогать; `renderers/tokens.ts` не менять
- [x] 6.5 `README.md`: как хост включает тему (атрибут, прямой
      `color-scheme`, «следовать системе» через `light dark`) и почему по
      умолчанию светло
      → плюс шапка в `docs/theming.md`: тот документ описывает старую семью, и
      без пометки он читался бы как инструкция для всего каталога
