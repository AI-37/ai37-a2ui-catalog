## 1. Схема

- [x] 1.1 Добавить опциональный `draftAction` в `liftEditorPropsSchema`
  (`packages/catalog-schemas/src/components/lift-editor.ts`), аддитивно —
  `CATALOG_VERSION` остаётся v2
- [x] 1.2 Юнит-тесты схемы: props без `draftAction` валиден; с непустым именем
  валиден; с пустой строкой — отвергается

## 2. Фикстуры

- [x] 2.1 `fixtures/invalid`: `lift-editor-empty-draft-action.json`
- [x] 2.2 Проверить, что валидные фикстуры остаются без `draftAction` (базовое
  поведение без автосейва — путь отката), включить проп в тестах через
  override props

## 3. Рендерер: отправка черновика

- [x] 3.1 `lift-editor.tsx`: `sendDraft(next)` рядом с `handleSubmit` —
  общий построитель контекста `{method, building, lifts}` для обоих
- [x] 3.2 Дедупликация: хранить сериализацию последнего отправленного
  черновика, молчать при совпадении (design.md, Решение 6)
- [x] 3.3 Вызовы на границах экранов: смена вкладки, добавление лифта,
  удаление лифта, смена методики (design.md, Решение 2)
- [x] 3.4 Черновик не блокируется незаполненными обязательными полями
  (в отличие от submit'а) — Решение 5

## 4. Рендерер: blur изменённого поля

- [x] 4.1 `lift-editor-field.tsx`: запоминать значение на `focus`, на `blur`
  звать `onCommit`, только если значение изменилось (design.md, Решение 3)
- [x] 4.2 `lift-editor-screen.tsx`: проброс `onCommit` от поля наверх
  (типы — в `lift-editor.types.ts`)
- [x] 4.3 `lift-editor.tsx`: `onCommit` дёргает `sendDraft`; без `draftAction`
  обработчик — no-op

## 5. Тесты рендерера

- [x] 5.1 Без `draftAction` наружу по-прежнему уходит ровно один action —
  submit
- [x] 5.2 Смена вкладки, добавление и удаление лифта, смена методики — по
  одному черновику на каждое действие
- [x] 5.3 Смена методики: в контексте черновика `method` и значения ВНОВЬ
  выбранной ветки
- [x] 5.4 Blur изменённого поля шлёт черновик; blur без правки — не шлёт
- [x] 5.5 Дедупликация: правка + blur + смена вкладки = один action;
  переключение вкладок без правок новых action'ов не даёт
- [x] 5.6 Контекст черновика равен контексту submit'а на том же состоянии
- [x] 5.7 Неполный документ (незаполненное обязательное) уезжает черновиком,
  submit при этом заблокирован
- [x] 5.8 Черновик неактивной методики в контекст не попадает
- [x] 5.9 Регресс-прогон `lift-editor` и `constructions-editor` без изменений

## 6. Python-зеркало

- [x] 6.1 `draftAction` в `models/lift_editor.py` + parity-тест TS ↔ Python
- [x] 6.2 Невалидная фикстура пустого `draftAction` в python-тестах

## 7. Демо-стенд

- [x] 7.1 Добавить `draftAction` в витрины `LiftEditor` (`apps/demo`) по
  образцу `with-constructions-draft-action.ts`
- [ ] 7.2 Пройти вручную: убедиться по логу action'ов, что черновик уходит на
  границах экранов и на blur правки, и не уходит на проходе по полям табом

## 8. Публикация

- [ ] 8.1 `pnpm run export:public` + `pnpm run verify:public`, закоммитить
  артефакты
- [x] 8.2 `pnpm run version:bump <minor>` + CHANGELOG
- [x] 8.3 Полный прогон тестов (TS + Python), lint, typecheck
