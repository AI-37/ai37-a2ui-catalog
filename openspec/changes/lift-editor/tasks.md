## 1. Схема компонента

- [x] 1.1 Экспортировать из `form-card.ts` базовый объект поля до `.refine`
  (`formFieldBaseSchema`) — аддитивно, поведение FormCard не меняется
- [x] 1.2 Создать `packages/catalog-schemas/src/components/lift-editor.ts`:
  `liftEditorFieldSchema` (расширение базового поля: `combo`, `advanced`,
  `hint`, `optionNotes`, `optionsBy`), `liftsMode`, `fieldScope`,
  `dependentRule`, `methodConfig`, `liftEditorProps` (эскиз — design.md,
  Решение 11)
- [x] 1.3 Добавить refine: `method` из props MUST иметь конфиг в
  `methodConfigs`; `methodField.type === 'select'`; длина `when` каждой строки
  MUST совпадать с длиной `sources` правила
- [x] 1.4 Зарегистрировать компонент: `catalog.ts` (`componentDefinitions`),
  `constants.ts` (`CATALOG_COMPONENT_NAMES`), `index.ts` (export)
- [x] 1.5 Юнит-тесты схемы: валидный props проходит; неизвестная `method`,
  пустой `methodConfigs`, рассогласование `when`/`sources`, лишнее поле
  (strict) — отвергаются

## 2. Фикстуры

- [x] 2.1 `fixtures/valid`: методика с повторяемыми лифтами
  (`liftsMode: 'per-lift'`, два лифта, правило `Vn → h/t123`,
  `onNoMatch: 'keep'`, поля `advanced` на экране здания)
- [x] 2.2 `fixtures/valid`: методика с одной лифтовой группой
  (`liftsMode: 'group'`, `optionsBy` по типу здания, правила `Q → Pk` и
  `(тип здания, doorWidth, Vn) → tOst` с `onNoMatch: 'clear'`)
- [x] 2.3 `fixtures/invalid`: `method` без конфига; `liftFields: []`;
  `when` длиннее `sources`
- [x] 2.4 `fixtures/messages`: сквозное A2UI-сообщение с `LiftEditor`
- [x] 2.5 Зафиксировать размер валидной фикстуры в тесте (риск роста
  сообщения из-за конфигов всех методик и строк `dependentRules` — design.md,
  Risks)

## 3. Рендерер: каркас и вкладки

- [x] 3.1 `packages/catalog-react/src/renderers/lift-editor.types.ts` — типы
  props и внутреннего state (черновики по `method`, ручные правки зависимых
  полей)
- [x] 3.2 `lift-editor.tsx`: локальный state из props, выбор активного
  конфига по `method`, разводка экранов
- [x] 3.3 `lift-editor-tabs.tsx`: вкладки «Здание» + лифты, переключение без
  `dispatchAction`, пометка вкладок с незаполненными обязательными полями
- [x] 3.4 Выбор активной вкладки при монтировании (первая с незаполненными
  обязательными; иначе последняя вкладка лифта)
- [x] 3.5 `lift-editor-screen.tsx`: рендер списка полей через контролы
  FormCard; `methodField` — первым на экране здания
- [x] 3.6 `lift-editor-field.tsx`: `combo` (свободный ввод + подсказки,
  `optionNotes`), разрешение `optionsBy` по значению управляющего поля
- [x] 3.7 `lift-editor-advanced.tsx`: экспандер `advancedLabel` с
  инвариантами «пустое обязательное не прячем» и «меньше трёх полей — без
  блока» (design.md, Решение 9)
- [x] 3.8 Зарегистрировать рендерер в `packages/catalog-react/src/catalog.ts`

## 4. Рендерер: поведение

- [x] 4.1 `apply-dependent-rules.ts`: поиск строки по значениям `sources` в
  порядке объявления, перезапись `set`, `onNoMatch: keep | clear`, источники
  со `scope: 'building'` (design.md, Решение 7)
- [x] 4.2 Учёт ручных правок зависимых полей: правка не перетирается, пока не
  изменится хотя бы один источник правила
- [x] 4.3 Добавление и удаление лифта локально: дефолты из `liftFields`,
  перенумерация вкладок, запрет удаления последнего, учёт `maxLifts`
- [x] 4.4 Переключение `method`: смена активного конфига, перестроение полей
  и вкладок, скрытие add/remove при `liftsMode: 'group'`
- [x] 4.5 Черновики по методикам: раздельное хранение, восстановление при
  возврате, отсутствие переноса значений по именам полей (Решение 4)
- [x] 4.6 `find-missing-required.ts`: список незаполненных обязательных полей
  по вкладкам; блокировка submit
- [x] 4.7 Submit: один `dispatchAction` с `{method, building, lifts}` только
  активной ветки; других действий наружу нет (Решение 10)

## 5. Демо-стенд

- [x] 5.1 Превью `LiftEditor` в `apps/demo` на валидных фикстурах: обе
  методики, лог отправленного action'а
- [ ] 5.2 Пройти вручную сценарии спеки: вкладки, добавление и удаление
  лифта, смена методики, свободный ввод вне ряда, зависимые значения по
  нескольким источникам, экспандер, submit — убедиться, что сетевых
  обращений до submit нет
- [x] 5.3 Сверить с эталоном `mockup.html` (в папке change'а)

## 6. Python-зеркало

- [x] 6.1 Pydantic-модели `LiftEditor` в `packages/catalog-python` + экспорт
- [x] 6.2 Parity-тест TS ↔ Python на валидных и невалидных фикстурах

## 7. Тесты рендерера

- [x] 7.1 Вкладки: переключение без `dispatchAction`, сохранение введённого
- [x] 7.2 Добавление и удаление лифта: перенумерация, запрет удаления
  последнего, предел `maxLifts`
- [x] 7.3 Смена методики: перестроение полей и вкладок, режим группы
- [x] 7.4 Черновики по методикам: восстановление и независимость одноимённых
  полей
- [x] 7.5 `combo`: значение вне ряда принимается и уходит в submit; выбор
  подсказки триггерит зависимое правило
- [x] 7.6 `optionsBy`: смена управляющего поля меняет подсказки и не
  сбрасывает введённое значение
- [x] 7.7 Зависимые правила: один источник; источник со `scope: 'building'`;
  `onNoMatch: keep` и `clear`; сохранение ручной правки; возврат
  авто-подстановки при смене источника
- [x] 7.8 Экспандер: сворачивание при трёх и более полях, вынос пустого
  обязательного наружу, отсутствие блока при одном-двух полях
- [x] 7.9 Валидация: пометка вкладок и блокировка submit, снятие блокировки,
  значение вне нормативного ряда submit не блокирует
- [x] 7.10 Submit: ровно один action за всю сессию редактирования
- [x] 7.11 Регресс-прогон `FormCard` и `ConstructionsEditor` без изменений

## 8. Публикация

- [x] 8.1 `pnpm run export:public` — обновить `public/a2ui/catalogs` (v2,
  аддитивно), закоммитить артефакты
- [x] 8.2 `pnpm run version:bump <minor>` + CHANGELOG
- [x] 8.3 Полный прогон `pnpm run test` (TS + Python), lint, typecheck
