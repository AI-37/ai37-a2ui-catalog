## 1. Инвентаризация до кода

- [x] 1.1 Выписать секции экрана `KeoReport` по коду и отнести каждую к
      «закрыто набором» / «берём из библиотеки» / «новое» (`proposal.md`)
- [x] 1.2 Подтвердить числами, что уходит: строки стилей, классы, своя кнопка
- [x] 1.3 Проверить вывод «новых примитивов ноль» сборкой, а не на глаз

## 2. Наполнение

- [x] 2.1 Загрузить `fixtures/valid/keo-report-fail.json` и
      `keo-report-pass.json`; выдуманных данных не добавлять
- [x] 2.2 Разбирать схемой (`keoReportPropsSchema.parse`), а не приводить типом

## 3. Страница

- [x] 3.1 `report-assembly.types.ts` — вариант `KeoReportBlock`
- [x] 3.2 `report-block.tsx` — ветка `kind === 'keo'`, экран из пакета, а не
      его копия
- [x] 3.3 `report-assembly-fixture.ts` — два блока под лифтовым отчётом
- [x] 3.4 Лид страницы описывает пять наполнений
- [x] 3.5 `/proba/system` не трогать: новых примитивов нет

## 4. Ревью глазами

- [x] 4.1 «Что изменить» КЕО и лифтов рядом — расхождения выписаны решениями
      2–5 `design.md`
- [x] 4.2 «Помещения»: значение пилюлей, норма в `detail` (решение 6)
- [x] 4.3 «Скачать» — меню, как у двух других отчётов (решение 7)
- [x] 4.4 Скриншоты обоих наполнений — `preview/`
- [x] 4.5 Узкий контейнер 380 px: правый слот под титулом, страница вбок не
      едет — `preview/narrow-380-*.png`
- [x] 4.6 Гейт пользователя: посмотрел, принято

## 5. Дальше

- [x] 5.1 Решения перенесены в change переноса (`keo-report-next`)
