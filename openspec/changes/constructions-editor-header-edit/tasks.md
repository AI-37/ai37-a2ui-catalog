## 1. Обобщение единственной формы (catalog-react)

- [x] 1.1 `constructions-editor.tsx` + `constructions-editor.types.ts`:
      `editingLayer` → `editingTarget: {entryId, target: number | 'new' |
      'header' | 'passport'} | null`; карточка получает свой target пропом,
      открытие любой формы закрывает текущую любого вида

## 2. Шапка карточки: чтение ↔ форма (catalog-react)

- [x] 2.1 Компонент шапки `constructions-editor-card-header.tsx`: режим
      чтения — тип (с разновидностью) и название текстом + кнопка
      «Изменить»; форма — селекты типа/subtype, инпут названия, «Сохранить»
      / «Отмена»; локальная копия в `useState` при открытии
- [x] 2.2 «Сохранить» без изменений (поле-в-поле: type, subtype, name) —
      как «Отмена», без коммита; с изменениями → `onChange(entry, {commit:
      true})`; заголовок-аккордеон карточки во время набора не меняется

## 3. Паспортное Rпр: чтение ↔ форма (catalog-react)

- [x] 3.1 Элемент `constructions-editor-passport.tsx`: чтение — «Rпр по
      паспорту: N» (не задано — предупреждающим цветом) + «Изменить»;
      форма — числовой инпут, «Применить» / «Отмена», коммит по аналогии
      с шапкой

## 4. Тесты, демо, публикация

- [x] 4.1 Обновить `tests/react/constructions-editor.test.tsx`: шапка —
      текст по умолчанию; ввод в форме не меняет заголовок/чип и не шлёт
      action'ов; «Сохранить»/«Отмена»; смена типа фиксируется по коммиту;
      паспорт — чтение/форма/«Применить»; одна форма на редактор для всех
      видов (шапка закрывает слой); черновики по коммитам шапки/паспорта,
      без изменений — не шлются
- [ ] 4.2 `apps/demo`: проверить витрину руками (шапка, паспорт, черновики
      в консоли)
- [x] 4.3 `pnpm run test:ts`, python-тесты через uv, `pnpm run typecheck`,
      `pnpm run build`
- [ ] 4.4 Minor bump + CHANGELOG (только catalog-react и демо); тарболы в
      `~/github/ai37/.local-packages`; переустановка в spai-ui (overrides)
