# tasks — next-label-subscripts

## 1. Хелпер

- [ ] 1.1 `render-label-subscripts.tsx`: правило «одна буква + `_` + индекс
      1–3 буквы/цифры» по границам токена (design, Решение 1), `<sub
      className="a2ui-field__index">`
- [ ] 1.2 Юнит-тесты правила: `d_п`, `h_пд`, `Δ_ст`, `C_N`, `r_0`, `ε_зд`
      рендерятся; `some_word`, `ГСОП`, `MF` — нет

## 2. Примитивы

- [ ] 2.1 `field.tsx`: строковый `label` через хелпер, JSX — мимо
- [ ] 2.2 `summary-collapsible.tsx`: строковый `summary` через хелпер

## 3. Проверка по редакторам

- [ ] 3.1 `keo-editor-next`: подпись поля «d_п — глубина помещения, м» и
      заголовок свёртки «Геометрия помещения» показывают подстрочные индексы
- [ ] 3.2 `lift-editor-next`: сводки не сломаны, снапшоты обновлены
- [ ] 3.3 constructions (захардкоженные JSX-подписи): без изменений
