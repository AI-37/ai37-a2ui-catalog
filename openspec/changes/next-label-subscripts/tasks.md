# tasks — next-label-subscripts

## 1. Хелпер

- [x] 1.1 `render-label-subscripts.tsx`: правило «одна буква + `_` + индекс
      1–3 буквы/цифры» по границам токена (design, Решение 1), `<sub
      className="a2ui-field__index">`
- [x] 1.2 Юнит-тесты правила: `d_п`, `h_пд`, `Δ_ст`, `C_N`, `r_0`, `ε_зд`
      рендерятся; `some_word`, `ГСОП`, `MF` — нет

## 2. Примитивы

- [x] 2.1 `field.tsx`: строковый `label` через хелпер, JSX — мимо
- [x] 2.2 `summary-collapsible.tsx`: строковый `summary` через хелпер
- [x] 2.3 `section-item.tsx`: строковая сводка секции через хелпер — именно ею
      пользуются КЕО и лифт (design, Решение 4)
- [x] 2.4 `keo-next-field.tsx`: подпись собирается JSX-ом, строку прогоняет
      рендерер (design, Решение 4)

## 3. Проверка по редакторам

- [x] 3.1 `keo-editor-next`: подпись поля «d_п — глубина помещения, м» и
      заголовок свёртки «Геометрия помещения» показывают подстрочные индексы
- [x] 3.2 `lift-editor-next`: сводки не сломаны, снапшоты обновлены
- [x] 3.3 constructions (захардкоженные JSX-подписи): без изменений

## 4. Подписи под контролом (по решению владельца)

- [x] 4.1 `source-note.tsx`: обоснование агента через хелпер, результат в
      обёртке — иначе `inline-flex` с зазором разносит куски (design, Решение 6)
- [x] 4.2 Подписи КЕО: `keo-next-option-note` (`C_N`), `keo-next-value-warning`,
      `keo-next-warnings` (`d_п/h₀₁`), ветка подсказки в `keo-next-source-note`
- [x] 4.3 `lift-next-field-note`: подсказка тем же правилом — симметрия
- [x] 4.4 На экране КЕО не осталось ни одного `_` (проверено обходом текстовых
      узлов в браузере)
