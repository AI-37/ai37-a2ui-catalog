## 1. Контракт

- [x] 1.1 `constructionEntrySchema`: `tvRoom`, `totRoom` — `z.number().optional()`
- [x] 1.2 Python-зеркало
- [x] 1.3 Фикстуры: valid (лестничная клетка + перекрытие над подвалом),
      invalid (`tvRoom: null` — незаданное выражается отсутствием ключа)

## 2. Расчёт на клиенте

- [x] 2.1 `compute-nt.ts` — (5.3), `null` при отсутствии поправки
- [x] 2.2 `effective-rnorm.ts` — `Rтр · nt`
- [x] 2.3 `ConstructionsClimateBase` прокинут из редактора в список и карточку
- [x] 2.4 Чип Rпр сравнивает с нормой конструкции; счётчик футера — тоже

## 3. Карточка

- [x] 3.1 Два поля температур в форме шапки, у любого типа
- [x] 3.2 Плейсхолдеры — значения здания
- [x] 3.3 Чип `nt 0.82` рядом с чипом Rпр, только при вычислимой поправке
- [x] 3.4 Сводка шапки печатает заданные температуры
- [x] 3.5 Коммит снимает ключи при пустых полях; смена типа температуры
      сохраняет (в отличие от r)

## 4. Тесты

- [x] 4.1 `compute-nt`: числа приложения К СП 50 (0,913 и 0,519), пустой ввод,
      неполный климат, негодная пара
- [x] 4.2 Схема: valid принимается, `tvRoom: null` отвергается (TS и Python)
- [x] 4.3 Рендерер: плейсхолдеры, ввод tв*, ввод tот*, отсутствие чипа без
      ввода, поля у изделия, очистка

## 5. Гейт

- [x] 5.1 `pnpm run typecheck`, `pnpm run test:ts`, Python-тесты
- [x] 5.2 `pnpm run export:public && pnpm run verify:public`, `public/` закоммичен
- [x] 5.3 `pnpm run version:bump 0.36.0` + раздел в `CHANGELOG.md`
- [ ] 5.4 Ревью на стенде (этап 6) — гейт владельца
- [ ] 5.5 Публикация 0.36.0, потребители на версию из реестра
