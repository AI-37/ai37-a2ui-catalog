# tasks — keo-report-chip-subscripts

## 0. Пререквизит

- [ ] 0.1 Хелпер `renderLabelSubscripts` влит (`next-label-subscripts`)

## 1. Карточка «Исходные данные»

- [ ] 1.1 `keo-report-inputs.tsx`: `chip.label` через хелпер
- [ ] 1.2 `group.label` и `group.note` через хелпер; `chip.value` — как есть

## 2. Тесты

- [ ] 2.1 Чип `d_п · b_п · h, м` рендерится с `<sub>`, `_` не виден
- [ ] 2.2 Чип `Ориентация` и значения чипов — байт в байт как раньше
- [ ] 2.3 Строка с `$...$` (агент ещё не обновился) проходит без изменений —
      хелпер доллары не трогает
