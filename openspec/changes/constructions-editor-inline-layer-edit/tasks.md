## 1. Строка слоя: режимы summary/edit (catalog-react)

- [x] 1.1 `constructions-editor-layer-row.tsx`: режим `summary` — строка
      «№ · материал · толщина · λ» (значок «авто» / значение ручной /
      предупреждение «λ не задана» цветом; у зазоров — прежняя пометка про
      Rs), кликабельна и доступна с клавиатуры (role/tabIndex/Enter)
- [x] 1.2 Режим `edit`: локальная копия слоя в `useState` при открытии; ввод
      меняет только копию (lookup материала работает по копии); кнопки
      «Применить» (отдаёт копию через `onCommit`), «Отмена», «Удалить слой»
- [x] 1.3 Форма нового слоя: тот же компонент с пустым слоем и кнопкой
      «Добавить»; типы пропов в `constructions-editor.types.ts`

## 2. Карточка: один редактируемый слой и «+ Слой» (catalog-react)

- [x] 2.1 `constructions-editor-card.tsx`: `editingIndex: number | 'new' |
      null` пропом (state — в редакторе: форма одна на весь редактор, не на
      карточку); клик по строке переключает форму (правки прежней
      отбрасываются), «+ Слой» открывает форму нового слоя вместо вставки
      пустой строки в state
- [x] 2.2 Коммиты формы поднимаются наверх явными событиями: применить с
      изменениями / добавить / удалить слой → `onChange(entry, {commit:
      true})` (или отдельный колбэк) — редактору больше не нужно сравнение
      длин `layers`

## 3. Черновик по коммитам (catalog-react)

- [x] 3.1 `constructions-editor.tsx`: `sendDraft` вызывается по явному
      признаку коммита из карточки; удалить эвристику
      `current.layers.length !== next.layers.length`; add/remove конструкции —
      как прежде
- [x] 3.2 «Применить» без изменений (deep-equal копии и исходного слоя) не
      шлёт черновик

## 4. Подсветка невалидной конструкции (catalog-react)

- [x] 4.1 Чистая функция `find-invalid-layers.ts` (рядом с
      `compute-live-rpr.ts`): материальный слой без λ/толщины/материала;
      для типов без слоёв — отсутствие `rprPassport`
- [x] 4.2 Карточка: предупреждающая подсветка (цвет из `tokens`) и пометка
      «проверить» у заголовка — в свернутом и раскрытом виде; предупреждение
      в невалидных строках-сводках; без блокировки submit и без action'ов

## 5. Тесты, демо, публикация

- [x] 5.1 Обновить `tests/react/constructions-editor.test.tsx` под новый UX:
      слои свернуты по умолчанию; одна форма; «Применить» коммитит и шлёт
      один черновик, без изменений — не шлёт; ввод в форме не меняет live-Rпр
      и не шлёт action'ов; «Отмена» и переключение строки отбрасывают правки;
      «+ Слой»/«Добавить»/«Отмена»; удаление слоя из формы; подсветка
      невалидности появляется и гаснет; регрессия lookup'а материала
- [x] 5.2 `apps/demo`: комментарий логгера черновиков («по коммитам» вместо
      «структурных правок»), проверить витрину руками
- [x] 5.3 `pnpm run test`, `pnpm run typecheck`, `pnpm run build`
- [x] 5.4 Minor bump + CHANGELOG (схемы/python не меняются — только
      catalog-react и демо); тарболы в `~/github/ai37/.local-packages`
