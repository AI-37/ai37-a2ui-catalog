## 1. Схема и зеркала

- [x] 1.1 `general.gsop: z.number().nullable().optional()` в
  `constructions-editor.ts` + тип
- [x] 1.2 `fieldSourceSchema` (`source`: project | question | suggested |
  default, `note` ≤200) и `generalSources` — `.strict()`-объект с ключами
  `ConstructionsGeneral`, все опциональные
- [x] 1.3 `conditionsCollapsed`, `headerTitle`, `headerContext` (все
  опциональные); `generalTabLabel` / `constructionsTabLabel` / `nextLabel`
  помечены `@deprecated` в комментариях схемы
- [x] 1.4 Обновить `description` в `constructionsEditorDefinition`
- [x] 1.5 Pydantic-зеркало новых моделей в `catalog-python` + parity-тест
- [x] 1.6 Тесты схемы: валидный `generalSources`, отказ на неизвестном ключе,
  старые фикстуры остаются валидными

## 2. CSS-слой (без изменения поведения)

- [x] 2.1 Новый файл стилевой константы пакета: CSS-строка с префиксом
  классов `a2ui-ce-`, узкая раскладка базовая, широкая — внутри
  `@container (min-width: 560px)`
- [x] 2.2 Компонент `<StyleTag>` — `<style href precedence>` (React 19
  hoisting), рендерится из корня `ConstructionsEditor`
- [x] 2.3 Корень редактора: `container-type: inline-size` + имя контейнера
- [x] 2.4 Перевести `constructions-editor.tsx`,
  `constructions-editor-card.tsx`, `-card-header.tsx`, `-layer-row.tsx`,
  `-passport.tsx`, `-general.tsx` с инлайновых стилей на классы
- [x] 2.5 Убрать применения `FIELD_COLUMN_WIDTH` в файлах
  `constructions-editor-*` (саму константу оставить — её использует
  `LiftEditor`)
- [x] 2.6 Починить тесты `tests/react`, которые смотрят на инлайн-стиль
- [x] 2.7 Проверить визуально: снимки headless Chrome по отрендеренной разметке
  (широкий и узкий контейнер) — раскладка совпадает

## 3. Визуальное соответствие макету

- [x] 3.1 Снять значения с макета — готово, см. `figma-values.md`
- [x] 3.2 Завести оттенки макета токенами `--a2ui-*` с fallback'ами в
  `tokens.ts` (палитра тёплая, с текущей slate почти не пересекается);
  литералов в CSS не оставлять
- [x] 3.3 Заголовки секций капителью, контролы с заливкой и единым радиусом,
  поле `project` — приглушённая заливка при той же рамке (по макету)
- [x] 3.4 Подпись источника: кегль, цвет по источнику, совпадение с цветом
  рамки контрола
- [x] 3.5 Шапка карточки: разделительная линия, заголовок и контекст на одной
  базовой линии
- [x] 3.6 Сверка скриншотом с макетом: свёрнутый блок, раскрытые условия с
  provenance, форма слоя — широкий и узкий контейнер

## 4. Адаптив

- [x] 4.1 Сетка условий: 1 колонка ниже брейкпоинта, 2 — выше
- [x] 4.2 Ряд «толщина + λ» формы слоя: 1 колонка ниже брейкпоинта
- [x] 4.3 Тест: нет горизонтального переполнения на контейнере 320px
- [x] 4.4 Тест: раскладка зависит от контейнера, не от окна (узкий контейнер в
  широком окне)

## 5. Блок «Условия» вместо табов

- [x] 5.1 Удалить рендер табов из `constructions-editor.tsx`; `activeTab` →
  `conditionsOpen` с начальным значением из `conditionsCollapsed`
- [x] 5.2 Удалить кнопку «Далее» (`nextLabel` игнорируется)
- [x] 5.3 Шапка карточки: `headerTitle` слева, `headerContext` справа; без
  `headerTitle` шапки нет
- [x] 5.4 Свёрнутая строка: заголовок «Условия расчёта», сводка, «Показать»;
  новый файл сборки сводки из `general` (пустые значения опускаются)
- [x] 5.5 Раскрытый блок: сетка из четырёх полей (регион+ГСОП, назначение, tв,
  условия эксплуатации); `buildingType` — только новая подпись «Назначение
  помещений» и место, контракт поля не меняется
- [x] 5.6 Ряд «Климат» (`tot`/`zot`/`tn`) ниже основной сетки
- [x] 5.7 ГСОП в поле региона; при `climateDirty` скрыт
- [x] 5.8 Тесты: переключение не шлёт action, введённое переживает
  сворачивание, сводка следует за правкой, `conditionsCollapsed` управляет
  начальным состоянием, старые пропы табов не рисуются

## 6. Provenance

- [x] 6.1 `Set` тронутых ключей `general`; сброс там же, где сбрасывается
  `general` при смене `baseClimate`
- [x] 6.2 Подпись источника под контролом: текст `note` или название источника
- [x] 6.3 Оформление по источнику: `project` — приглушённый контрол,
  `question`/`suggested` — акцентная рамка, `default` — маркер
- [x] 6.4 Ни один источник не делает поле недоступным для ввода
- [x] 6.5 Правка снимает подпись и акцент только у тронутого поля
- [x] 6.6 Тесты: отрисовка по каждому источнику, источник без `note`, снятие
  после правки, соседние поля не затронуты, `generalSources` не попадает в
  payload submit/draft

## 7. Фикстуры, демо, релиз

- [x] 7.1 Новая валидная фикстура: `generalSources`, `gsop`,
  `conditionsCollapsed: true`, шапка карточки
- [x] 7.2 Сообщение в `fixtures/messages` для демо
- [x] 7.3 Проверка скриншотами: широкий контейнер, узкий контейнер,
  свёрнутый/раскрытый блок, подписи источников, форма слоя
- [ ] 7.4 Проверить SSR-рендер `<style href precedence>` у потребителя на
  Next.js (`spai-ui`) — юнит-тесты этого не ловят.
  Замер здесь: `renderToStaticMarkup(<A2uiSurface/>)` падает ещё до наших
  стилей — `A2uiSurface` (@a2ui/react 0.10.1) зовёт `useSyncExternalStore`
  без `getServerSnapshot`. Значит surface у потребителя в любом случае
  рисуется на клиенте, и проверять надо его интеграцию, а не наш тег
- [x] 7.5 `pnpm run test`, `pnpm run lint`, `pnpm run build`
- [x] 7.6 `pnpm run version:bump <minor>` + запись в `CHANGELOG.md`
